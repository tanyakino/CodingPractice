#include <stdio.h>

int main ()
{
	int w, h;

	printf ("Enter width: ");
	scanf ("%d", &w);
	printf ("Enter height: ");
	scanf ("%d", &h);

	for(int i = 1; i <= w; ++i)
		{
			printf ("* ");
		}

		for ()

	return 0;
}